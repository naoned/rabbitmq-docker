version: '3'
services:

    rabbitmq:
        image: rabbitmq:3.6-management
        container_name: rabbitmq
        ports:
            - "15672:15672"
            - "5672:5672"
        networks:
            - naoned

networks:
  naoned:
    external: true
